<properties 
	pageTitle="Как ограничить доступ к содержимому по стране" 
	description="При запросе содержимого по умолчанию оно возвращается пользователю независимо от того, откуда был сделан запрос. В некоторых случаях может потребоваться ограничить доступ к содержимому по стране. В этом разделе объясняется, как настроить службу для разрешения или запрета доступа в зависимости от страны." 
	services="cdn" 
	documentationCenter=".net" 
	authors="zhangmanling" 
	manager="dwrede" 
	editor=""/>

<tags 
	ms.service="cdn" 
	ms.workload="tbd" 
	ms.tgt_pltfrm="na" 
	ms.devlang="na" 
	ms.topic="article" 
	ms.date="07/16/2015" 
	ms.author="mazha"/>

#Как ограничить доступ к содержимому по стране
 
 
При запросе содержимого по умолчанию оно возвращается пользователю независимо от того, откуда был сделан запрос. В некоторых случаях может потребоваться ограничить доступ к содержимому по стране. В этом разделе объясняется, как настроить службу для разрешения или запрета доступа в зависимости от страны.

>[AZURE.NOTE]После настройки конфигурации она будет применена ко всем конечным точкам CDN в вашей подписке.

##Шаг 1. Определение пути к каталогу 

При настройке фильтра страны необходимо указать относительный путь к каталогу, доступ к которому будет разрешен или запрещен. Фильтрацию по стране можно применить ко всем файлам в корневом каталоге («/») или к выбранным каталогам, указав пути к этим каталогам.

Пример фильтра по каталогу:
		
	/                                 
	/Photos/
	/Photos/Strasbourg

##Шаг 2. Определение действия: запрет или разрешение

**Запрет**: пользователям из указанных стран будет запрещен доступ к ресурсам, запрашиваемым по указанному рекурсивному пути. Если для этого пути не было настроено другой фильтрации по стране, то доступ для всех остальных пользователей будет разрешен.

**Разрешение**: доступ к ресурсам, запрашиваемым по указанному рекурсивному пути, будет разрешен только пользователям из указанных стран.
	 
##Шаг 3. Определение стран 

Выберите страны, доступ из которых для выбранного пути нужно разрешить или запретить.

Пример:

Правило запрета доступа **/Photos/Strasbourg/** отфильтрует файлы, включая следующие:

	http://az123456.vo.msecnd.net/Photos/Strasbourg/1000.jpg. 
	http://az123456.vo.msecnd.net/Photos/Strasbourg/Cathedral/1000.jpg. 

##Коды стран

Функция **Фильтрации по странам** использует коды стран для определения стран, из которых запрос к защищаемому каталогу будет разрешаться или блокироваться. В настоящее время поддерживаются два следующих кода стран: EU (Европа) и AP (Азиатско-Тихоокеанский регион). Однако с помощью этих кодов невозможно разрешить или запретить все запросы из этих регионов. Вместо этого эти коды стран применяются к запросам, поступающим с IP-адресов, распределенных по территории региона, а не страны. Для разрешения или запрета доступа для стран Европы и Азиатско-Тихоокеанского региона выберите страны и соответствующий регион (EU ил AP).


##Рекомендации
 
- Для вступления в силу настроек фильтрации для вашей страны может потребоваться около часа.
- Эта функция не поддерживает подстановочные знаки (например,*).- Настройки фильтрации по стране, связанные с относительным путем, будут применяться рекурсивно к этому пути.
- К одному и тому же относительному пути может применяться только одно правило (нельзя создать несколько фильтров стран, которые указывают на один и тот же относительный путь). Однако к одному и тому же каталогу могут применяться несколько фильтров стран. Это возможно благодаря рекурсивной природе фильтров стран. Другими словами, подкаталогу ранее настроенного каталога можно назначить другой фильтр страны.

<!---HONumber=July15_HO3-->