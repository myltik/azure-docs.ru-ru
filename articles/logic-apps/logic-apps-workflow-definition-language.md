---
title: Схема языка определения рабочих процессов в Azure Logic Apps | Документация Майкрософт
description: Определение рабочих процессов на основе соответствующей схемы для Azure Logic Apps
services: logic-apps
author: jeffhollan
manager: anneta
editor: ''
documentationcenter: ''
ms.assetid: 26c94308-aa0d-4730-97b6-de848bffff91
ms.service: logic-apps
ms.workload: integration
ms.tgt_pltfrm: na
ms.devlang: multiple
ms.topic: article
ms.date: 03/21/2017
ms.author: LADocs; jehollan
ms.openlocfilehash: 42932e6d1727a1444c62f565ae3c48dc178aeb2b
ms.sourcegitcommit: 59914a06e1f337399e4db3c6f3bc15c573079832
ms.translationtype: HT
ms.contentlocale: ru-RU
ms.lasthandoff: 04/20/2018
---
# <a name="workflow-definition-language-schema-for-azure-logic-apps"></a>Схема языка определения рабочих процессов в Azure Logic Apps

Определение рабочего процесса содержит фактическую логику, которая выполняется как часть приложения логики. Это определение включает в себя один или несколько триггеров для запуска приложения логики и одно или несколько действий, которые будет выполнять приложение логики.  
  
## <a name="basic-workflow-definition-structure"></a>Базовая структура определения рабочего процесса

Ниже представлена базовая структура определения рабочего процесса.  
  
```json
{
    "$schema": "<schema-of the-definition>",
    "contentVersion": "<version-number-of-definition>",
    "parameters": { <parameter-definitions-of-definition> },
    "triggers": [ { <definition-of-flow-triggers> } ],
    "actions": [ { <definition-of-flow-actions> } ],
    "outputs": { <output-of-definition> }
}
```
  
> [!NOTE]
> В документации [по REST API управления рабочими процессами](https://docs.microsoft.com/rest/api/logic/workflows) содержатся сведения о способах создания рабочих процессов приложений логики и управления ими.
  
|Имя элемента|Обязательно|ОПИСАНИЕ|  
|------------------|--------------|-----------------|  
|$schema|Нет |Расположение файла схемы JSON, который описывает версию языка определения. Это расположение требуется для ссылки на определение извне. Этот документ находится здесь: <p>`https://schema.management.azure.com/schemas/2016-06-01/Microsoft.Logic.json`|  
|contentVersion|Нет |Версия определения. Это значение позволяет гарантировать использование правильного определения при развертывании рабочего процесса с помощью определения.|  
|parameters|Нет |Параметры, используемые для ввода данных в определение. Можно определить до 50 параметров.|  
|триггеры;|Нет |Сведения о триггерах, которые запускают рабочий процесс. Можно определить до 10 триггеров.|  
|actions|Нет |Действия, предпринимаемые при выполнении потока. Можно определить до 250 действий.|  
|outputs|Нет |Сведения о развернутых ресурсах. Можно определить до 10 выводов.|  
  
## <a name="parameters"></a>Параметры

В этом разделе приведены все параметры, используемые в определении рабочего процесса во время развертывания. Чтобы параметры можно было использовать в других разделах определения, их нужно объявить в этом разделе.  
  
В следующем примере показана структура определения параметров:  

```json
"parameters": {
    "<parameter-name>" : {
        "type" : "<type-of-parameter-value>",
        "defaultValue": <default-value-of-parameter>,
        "allowedValues": [ <array-of-allowed-values> ],
        "metadata" : { "key": { "name": "value"} }
    }
}
```

|Имя элемента|Обязательно|ОПИСАНИЕ|  
|------------------|--------------|-----------------|  
|Тип|Yes|**Тип:** string. <p> **Объявление:** `"parameters": {"parameter1": {"type": "string"}}` <p> **Спецификация:** `"parameters": {"parameter1": {"value": "myparamvalue1"}}` <p> **Тип:** securestring. <p> **Объявление:** `"parameters": {"parameter1": {"type": "securestring"}}` <p> **Спецификация:** `"parameters": {"parameter1": {"value": "myparamvalue1"}}` <p> **Тип:** int. <p> **Объявление:** `"parameters": {"parameter1": {"type": "int"}}` <p> **Спецификация:** `"parameters": {"parameter1": {"value" : 5}}` <p> **Тип:** bool. <p> **Объявление:** `"parameters": {"parameter1": {"type": "bool"}}` <p> **Спецификация:** `"parameters": {"parameter1": { "value": true }}` <p> **Тип:** array. <p> **Объявление:** `"parameters": {"parameter1": {"type": "array"}}` <p> **Спецификация:** `"parameters": {"parameter1": { "value": [ array-of-values ]}}` <p> **Тип:** object. <p> **Объявление:** `"parameters": {"parameter1": {"type": "object"}}` <p> **Спецификация:** `"parameters": {"parameter1": { "value": { JSON-object } }}` <p> **Тип:** secureobject. <p> **Объявление:** `"parameters": {"parameter1": {"type": "object"}}` <p> **Спецификация:** `"parameters": {"parameter1": { "value": { JSON-object } }}` <p> **Примечание.** Типы `securestring` и `secureobject` не возвращаются в операциях `GET`. Этот тип нужно использовать для всех паролей, ключей и секретов.|  
|defaultValue|Нет |Значение по умолчанию для параметра, если во время создания ресурса не было указано значение.|  
|allowedValues|Нет |Массив допустимых значений параметра.|  
|metadata|Нет |Дополнительные сведения о параметре, например понятное описание или данные, касающиеся разработки, используемые Visual Studio или другими средствами.|  
  
В примере ниже показано, как использовать параметр в разделе body действия.  
  
```json
"body" :
{
  "property1": "@parameters('parameter1')"
}
```

 Параметры также можно использовать в разделе outputs.  
  
## <a name="triggers-and-actions"></a>Триггеры и действия  

Триггеры и действия указывают вызовы, которые могут участвовать в выполнении рабочего процесса. Дополнительные сведения об этом разделе см. в статье [Действия и триггеры рабочих процессов в Azure Logic Apps](logic-apps-workflow-actions-triggers.md).
  
## <a name="outputs"></a>outputs  

В разделе outputs указываются сведения, которые могут быть возвращены из выполнения рабочего процесса. Например, если для каждого выполнения необходимо отслеживать определенное состояние или значение, можно задать эти данные в разделе выходных данных выполнения. Выходные данные отображаются в REST API управления для этого выполнения и в пользовательском интерфейсе управления для этого выполнения на портале Azure. Эти данные также можно передать в другие внешние системы, например PowerBI для создания панелей мониторинга. Данные в разделе outputs *не* используются для ответа на входящие запросы в REST API службы. Для ответа на входящий запрос используйте тип действия `response`. Ниже приведен пример раздела outputs.
  
```json
"outputs": {  
  "key1": {  
    "value": "value1",  
    "type" : "<type-of-value>"  
  }  
} 
```

|Имя элемента|Обязательно|ОПИСАНИЕ|  
|------------------|--------------|-----------------|  
|key1|Yes|Идентификатор ключа для выходных данных. Замените **key1** именем для идентификации выходных данных.|  
|value|Yes|Значение выходных данных.|  
|Тип|Yes|Тип указанного значения. Возможные типы значений: <ul><li>`string`</li><li>`securestring`</li><li>`int`</li><li>`bool`</li><li>`array`</li><li>`object`</li></ul>|
  
## <a name="expressions"></a>Выражения  

Значения JSON в определении могут быть литералами или выражениями, которые вычисляются при использовании определения. Например:   
  
```json
"name": "value"
```

 или  
  
```json
"name": "@parameters('password') "
```

> [!NOTE]
> Некоторые выражения получают значения из действий среды выполнения, которые могут не существовать в начале выполнения. Для получения некоторых из этих значений можно использовать **функции**.  
  
Выражения могут встречаться в любом месте строкового значения JSON и всегда возвращают другое значение JSON. Если определено, что значение JSON является выражением, текст выражения извлекается без знака "@". Если требуется строковый литерал, начинающийся с @, эту строку необходимо экранировать с помощью "@@". В примерах ниже показано, как вычисляются выражения.  
  
|Значение JSON|Результат|  
|----------------|------------|  
|"parameters"|Возвращаются символы в виде 'parameters'.|  
|"parameters[1]"|Возвращаются символы в виде 'parameters[1]'.|  
|"@@"|Возвращается строка из 1 символа, содержащая символ \@.|  
|\"\@\"|Возвращается строка из 2 символов, содержащая символ  \@.|  
  
При *интерполяции строк* выражения также могут содержаться внутри строк, где они заключаются в структуру `@{ ... }`. Например:  <p>`"name" : "First Name: @{parameters('firstName')} Last Name: @{parameters('lastName')}"`

В результате всегда выводится строка, поэтому эта функция схожа с функцией `concat`. Предположим, что вы определили `myNumber` как `42`, а `myString` — как `sampleString`:  
  
|Значение JSON|Результат|  
|----------------|------------|  
|"@parameters('myString')"|Возвращает `sampleString` как строку.|  
|"@{parameters('myString')}"|Возвращает `sampleString` как строку.|  
|"@parameters('myNumber')"|Возвращает `42` как *номер*.|  
|"@{parameters('myNumber')}"|Возвращает `42` как *строку*.|  
|"Answer is: @{parameters('myNumber')}"|Возвращает строку `Answer is: 42`.|  
|"@concat('Answer is: ', string(parameters('myNumber')))"|Возвращает строку `Answer is: 42`.|  
|"Answer is: @@{parameters('myNumber')}"|Возвращает строку `Answer is: @{parameters('myNumber')}`.|  
  
## <a name="operators"></a>Операторы  

Операторы — это символы, которые можно использовать внутри выражения или функции. 
  
|Оператор|Описание|  
|--------------|-----------------|  
|.|Оператор "точка" позволяет ссылаться на свойства объекта.|  
|?|Оператор "вопросительный знак" позволяет ссылаться на отсутствующие свойства объекта без ошибок времени выполнения. Например, следующее выражение можно использовать для обработки отсутствующих выходных данных триггера: <p>`@coalesce(trigger().outputs?.body?.property1, 'my default value')`|  
|'|Для помещения строковых литералов в оболочку можно использовать только одинарные кавычки. Внутри выражений нельзя использовать двойные кавычки, так как такая пунктуация конфликтует с кавычками JSON, которые формируют оболочку для всего выражения.|  
|[]|Квадратные скобки можно использовать для получения значения из массива с указанным индексом. Например, если у вас есть действие, которое передает `range(0,10)` в функцию `forEach`, эту функцию можно использовать для получения элементов из массивов:  <p>`myArray[item()]`|  
  
## <a name="functions"></a>Functions  

Внутри выражений также можно вызывать функции. Ниже приведены функции, которые могут использоваться в выражении.  
  
|Expression|Оценка|  
|----------------|----------------|  
|"@function('Hello')"|Вызывает функцию-элемент определения со строковым литералом Hello в качестве первого параметра.|  
|"@function('It''s Cool!')"|Вызывает функцию-элемент определения со строковым литералом 'It's Cool!' в качестве первого параметра.|  
|"@function().prop1"|Возвращает значение свойства prop1 элемента `myfunction` определения.|  
|"@function('Hello').prop1"|Вызывает функцию-элемент определения со строковым литералом Hello в качестве первого параметра и возвращает свойство prop1 объекта.|  
|"@function(parameters('Hello'))"|Вычисляет параметр Hello и передает значение функции.|  
  
### <a name="referencing-functions"></a>Функции для ссылки  

Эти функции можно использовать для ссылки на выходные данные из других действий в приложении логики или на значения, переданные при создании приложения логики. Например, можно создать ссылку на данные на одном шаге, чтобы использовать их на другом.  
  
|Имя функции|ОПИСАНИЕ|  
|-------------------|-----------------|  
|parameters|Возвращает значение параметра, заданное в определении. <p>`parameters('password')` <p> **Номер параметра:** 1. <p> **Имя:** параметр. <p> **Описание:** обязательно. Имя параметра, значения которого требуются.|  
|action|Эта функция позволяет выражению получать его значение из других пар "имя JSON — значение" или выходных данных текущего действия среды выполнения. Свойство, представленное в propertyPath ниже, указывать необязательно. Если оно не указано, ссылка указывает на весь объект действия. Эта функция может использоваться только внутри условий do-until для действия. <p>`action().outputs.body.propertyPath`|  
|actions|Эта функция позволяет выражению получать его значение из других пар "имя JSON — значение" или выходных данных действия среды выполнения. Эти выражения явно объявляют, что одно действие зависит от другого. Свойство, представленное в propertyPath ниже, указывать необязательно. Если оно не указано, ссылка указывает на весь объект действия. Зависимости можно указать как с помощью этой функции, так и с помощью элемента conditions, но использовать оба варианта для одного зависимого ресурса не нужно. <p>`actions('myAction').outputs.body.propertyPath` <p> **Номер параметра:** 1. <p> **Имя:** имя действия. <p> **Описание:** обязательно. Имя действия, значения которого требуются. <p> К доступным свойствам объекта действия относятся: <ul><li>`name`</li><li>`startTime`</li><li>`endTime`</li><li>`inputs`</li><li>`outputs`</li><li>`status`</li><li>`code`</li><li>`trackingId`</li><li>`clientTrackingId`</li></ul> <p>Сведения об этих свойствах см. в статье для [Rest API](http://go.microsoft.com/fwlink/p/?LinkID=850646).|
|trigger|Эта функция позволяет выражению получать его значение из других пар "имя JSON — значение" или выходных данных триггера среды выполнения. Свойство, представленное в propertyPath ниже, указывать необязательно. Если оно не указано, ссылка указывает на весь объект триггера. <p>`trigger().outputs.body.propertyPath` <p>При использовании внутри входных данных триггера функция возвращает выходные данные предыдущего выполнения. Однако при использовании внутри условия триггера функция `trigger` возвращает выходные данные текущего выполнения. <p> К доступным свойствам объекта триггера относятся: <ul><li>`name`</li><li>`scheduledTime`</li><li>`startTime`</li><li>`endTime`</li><li>`inputs`</li><li>`outputs`</li><li>`status`</li><li>`code`</li><li>`trackingId`</li><li>`clientTrackingId`</li></ul> <p>Сведения об этих свойствах см. в статье для [Rest API](http://go.microsoft.com/fwlink/p/?LinkID=850644).|
|actionOutputs|Эта функция является сокращением для `actions('actionName').outputs`. <p> **Номер параметра:** 1. <p> **Имя:** имя действия. <p> **Описание:** обязательно. Имя действия, значения которого требуются.|  
|actionBody и body|Эти функции являются сокращением для `actions('actionName').outputs.body`. <p> **Номер параметра:** 1. <p> **Имя:** имя действия. <p> **Описание:** обязательно. Имя действия, значения которого требуются.|  
|triggerOutputs|Эта функция является сокращением для `trigger().outputs`.|  
|triggerBody|Эта функция является сокращением для `trigger().outputs.body`.|  
|item|При использовании внутри повторяющегося действия эта функция возвращает элемент, находящийся в массиве для данной итерации действия. Например, если имеется действие, которое выполняет для каждого элемента массив сообщений, можно использовать следующий синтаксис: <p>`"input1" : "@item().subject"`| 
  
### <a name="collection-functions"></a>Функции для коллекций  

Эти функции работают с коллекциями и обычно применяются к массивам, строкам и иногда к словарям.  
  
|Имя функции|ОПИСАНИЕ|  
|-------------------|-----------------|  
|contains|Возвращает значение true, если словарь содержит ключ, список содержит значение или строка содержит подстроку. Например, эта функция возвращает `true`: <p>`contains('abacaba','aca')` <p> **Номер параметра:** 1. <p> **Имя:** в коллекции. <p> **Описание:** обязательно. Коллекция, в которой будет выполняться поиск. <p> **Номер параметра:** 2. <p> **Имя:** объект поиска. <p> **Описание:** обязательно. Искомый объект внутри **коллекции**.|  
|длина|Возвращает количество элементов в массиве или строке. Например, эта функция возвращает `3`:  <p>`length('abc')` <p> **Номер параметра:** 1. <p> **Имя:** коллекция. <p> **Описание:** обязательно. Коллекция, для которой нужно получить длину.|  
|empty|Возвращает значение true, если объект, массив или строка пуста. Например, эта функция возвращает `true`: <p>`empty('')` <p> **Номер параметра:** 1. <p> **Имя:** коллекция. <p> **Описание:** обязательно. Коллекция, которую нужно проверить на наличие содержимого.|  
|пересечению|Возвращает массив или объект, имеющий общие элементы между переданными массивами или объектами. Например, эта функция возвращает `[1, 2]`: <p>`intersection([1, 2, 3], [101, 2, 1, 10],[6, 8, 1, 2])` <p>Параметрами для этой функции может быть набор объектов или набор массивов (не сочетание обоих). Если имеется два объекта с одинаковым именем, в конечном объекте содержится последний объект с таким именем. <p> **Номер параметра:** 1 ... *n*. <p> **Имя:** коллекция *n*. <p> **Описание:** обязательно. Коллекции для оценки. Для отображения в результате объект должен присутствовать во всех переданных коллекциях.|  
|union|Возвращает массив или объект со всеми элементами, содержащимися в массиве или объекте, переданном в эту функцию. Например, эта функция возвращает `[1, 2, 3, 10, 101]`: <p>`union([1, 2, 3], [101, 2, 1, 10])` <p>Параметрами для этой функции может быть набор объектов или набор массивов (не сочетание обоих). Если в конечных выходных данных имеется два объекта с одинаковым именем, в конечном объекте отображается последний объект с таким именем. <p> **Номер параметра:** 1 ... *n*. <p> **Имя:** коллекция *n*. <p> **Описание:** обязательно. Коллекции для оценки. Объект, который содержится в какой-либо коллекции, также отображается в результатах.|  
|first|Возвращает первый элемент в переданном массиве или строке. Например, эта функция возвращает `0`: <p>`first([0,2,3])` <p> **Номер параметра:** 1. <p> **Имя:** коллекция. <p> **Описание:** обязательно. Коллекция, из которой нужно извлечь первый объект.|  
|last|Возвращает последний элемент в переданном массиве или строке. Например, эта функция возвращает `3`: <p>`last('0123')` <p> **Номер параметра:** 1. <p> **Имя:** коллекция. <p> **Описание:** обязательно. Коллекция, из которой нужно извлечь последний объект.|  
|take|Возвращает первые **число** элементов из переданного массива или строки. Например, эта функция возвращает `[1, 2]`:  <p>`take([1, 2, 3, 4], 2)` <p> **Номер параметра:** 1. <p> **Имя:** коллекция. <p> **Описание:** обязательно. Коллекция, из которой нужно извлечь первые **число** объектов. <p> **Номер параметра:** 2. <p> **Имя:** число. <p> **Описание:** обязательно. Число объектов, которые нужно извлечь из **коллекции**. Принимаются только положительные целые числа.|  
|skip|Возвращает элементы в массиве, начиная с индекса **число**. Например, эта функция возвращает `[3, 4]`: <p>`skip([1, 2 ,3 ,4], 2)` <p> **Номер параметра:** 1. <p> **Имя:** коллекция. <p> **Описание:** обязательно. Коллекция, в которой нужно пропустить первые **число** объектов. <p> **Номер параметра:** 2. <p> **Имя:** число. <p> **Описание:** обязательно. Количество объектов, которые нужно удалить в начале **коллекции**. Принимаются только положительные целые числа.|  
|join|Возвращает строку, где все элементы массива объединены с помощью разделителей, например `"1,2,3,4"` возвращается как:<br /><br /> `join([1, 2, 3, 4], ',')`<br /><br /> **Номер параметра:** 1.<br /><br /> **Имя:** коллекция.<br /><br /> **Описание:** обязательно. Коллекция, элементы из которой нужно объединить.<br /><br /> **Номер параметра:** 2.<br /><br /> **Имя:** разделитель.<br /><br /> **Описание:** обязательно. Строка для отделения элементов.|  
  
### <a name="string-functions"></a>Строковые функции

Следующие функции применяются только к строкам. Вы также можете использовать в строках некоторые функции для коллекций.  
  
|Имя функции|ОПИСАНИЕ|  
|-------------------|-----------------|  
|concat|Объединяет любое число строк. Например, если параметр 1 — `p1`, эта функция возвращает `somevalue-p1-somevalue` так: <p>`concat('somevalue-',parameters('parameter1'),'-somevalue')` <p> **Номер параметра:** 1 ... *n*. <p> **Имя:** строка *n*. <p> **Описание:** обязательно. Строки, которые следует объединить в одну строку.|  
|substring|Возвращает подмножество символов из строки. Например, эта функция возвращает `abc`: <p>`substring('somevalue-abc-somevalue',10,3)` <p> **Номер параметра:** 1. <p> **Имя:** строка. <p> **Описание:** обязательно. Строка, из которой извлекается подстрока. <p> **Номер параметра:** 2. <p> **Имя:** индекс начала. <p> **Описание:** обязательно. Индекс, где начинается подстрока в параметре 1. <p> **Номер параметра:** 3. <p> **Имя:** длина. <p> **Описание:** обязательно. Длина подстроки.|  
|replace|Заменяет строку заданной строкой. Например, эта функция возвращает `the new string`: <p>`replace('the old string', 'old', 'new')` <p> **Номер параметра:** 1. <p> **Имя:** строка. <p> **Описание:** обязательно. Строка, в которой выполняется поиск параметра 2 и которая обновляется параметром 3, если параметр 2 найден в параметре 1. <p> **Номер параметра:** 2. <p> **Имя:** старая строка. <p> **Описание:** обязательно. Строка для замены параметром 3, если найдено совпадение в параметре 1. <p> **Номер параметра:** 3. <p> **Имя:** новая строка. <p> **Описание:** обязательно. Строка для замены строки в параметре 2, если найдено совпадение в параметре 1.|  
|GUID|Эта функция создает глобально уникальную строку (идентификатор GUID). Например, эта функция может создавать такой идентификатор GUID: `c2ecc88d-88c8-4096-912c-d6f2e2b138ce` <p>`guid()` <p> **Номер параметра:** 1. <p> **Имя:** формат. <p> **Описание:** необязательно. Один описатель формата, указывающий [способ форматирования значения GUID](https://msdn.microsoft.com/library/97af8hh4%28v=vs.110%29.aspx). Параметром формата может быть N, D, B, P или X. Если формат не указан, используется D.|  
|toLower|Преобразует строку в нижний регистр. Например, эта функция возвращает `two by two is four`: <p>`toLower('Two by Two is Four')` <p> **Номер параметра:** 1. <p> **Имя:** строка. <p> **Описание:** обязательно. Строка для преобразования в нижний регистр. Если символ в строке не имеет эквивалента в нижнем регистре, он добавляется в возвращаемую строку без изменений.|  
|toUpper|Преобразует строку в верхний регистр. Например, эта функция возвращает `TWO BY TWO IS FOUR`: <p>`toUpper('Two by Two is Four')` <p> **Номер параметра:** 1. <p> **Имя:** строка. <p> **Описание:** обязательно. Строка для преобразования в верхний регистр. Если символ в строке не имеет эквивалента в верхнем регистре, он добавляется в возвращаемую строку без изменений.|  
|indexof|Поиск индекса значения в строке без учета регистра. Например, эта функция возвращает `7`: <p>`indexof('hello, world.', 'world')` <p> **Номер параметра:** 1. <p> **Имя:** строка. <p> **Описание:** обязательно. Строка, которая может содержать значение. <p> **Номер параметра:** 2. <p> **Имя:** строка. <p> **Описание:** обязательно. Значение, для которого будет выполняться поиск индекса.|  
|lastindexof|Поиск последнего индекса значения в строке без учета регистра. Например, эта функция возвращает `3`: <p>`lastindexof('foofoo', 'foo')` <p> **Номер параметра:** 1. <p> **Имя:** строка. <p> **Описание:** обязательно. Строка, которая может содержать значение. <p> **Номер параметра:** 2. <p> **Имя:** строка. <p> **Описание:** обязательно. Значение, для которого будет выполняться поиск индекса.|  
|startswith|Проверяет, начинается ли строка с определенного значения без учета регистра. Например, эта функция возвращает `true`: <p>`startswith('hello, world', 'hello')` <p> **Номер параметра:** 1. <p> **Имя:** строка. <p> **Описание:** обязательно. Строка, которая может содержать значение. <p> **Номер параметра:** 2. <p> **Имя:** строка. <p> **Описание:** обязательно. Значение, с которого может начинаться строка.|  
|endswith|Проверяет, заканчивается ли строка определенным значением без учета регистра. Например, эта функция возвращает `true`: <p>`endswith('hello, world', 'world')` <p> **Номер параметра:** 1. <p> **Имя:** строка. <p> **Описание:** обязательно. Строка, которая может содержать значение. <p> **Номер параметра:** 2. <p> **Имя:** строка. <p> **Описание:** обязательно. Значение, которым может заканчиваться строка.|  
|split|Разбивает строку с помощью разделителя. Например, эта функция возвращает `["a", "b", "c"]`: <p>`split('a;b;c',';')` <p> **Номер параметра:** 1. <p> **Имя:** строка. <p> **Описание:** обязательно. Разбиваемая строка. <p> **Номер параметра:** 2. <p> **Имя:** строка. <p> **Описание:** обязательно. Разделитель.|  
  
### <a name="logical-functions"></a>Логические функции  

Эти функции подходят для условий и могут использоваться для определения логики любого типа.  
  
|Имя функции|ОПИСАНИЕ|  
|-------------------|-----------------|  
|equals (равно)|Возвращает значение true, если два значения равны. Например, если параметр 1 — someValue, эта функция возвращает `true`: <p>`equals(parameters('parameter1'), 'someValue')` <p> **Номер параметра:** 1. <p> **Имя:** объект 1. <p> **Описание:** обязательно. Объект для сравнения с **объектом 2**. <p> **Номер параметра:** 2. <p> **Имя:** объект 2. <p> **Описание:** обязательно. Объект для сравнения с **объектом 1**.|  
|less|Возвращает значение true, если первый аргумент меньше второго. Обратите внимание, значения могут быть только типа integer, float или string. Например, эта функция возвращает `true`: <p>`less(10,100)` <p> **Номер параметра:** 1. <p> **Имя:** объект 1. <p> **Описание:** обязательно. Объект для проверки, не меньше ли он **объекта 2**. <p> **Номер параметра:** 2. <p> **Имя:** объект 2. <p> **Описание:** обязательно. Объект для проверки, не больше ли он **объекта 1**.|  
|lessOrEquals|Возвращает значение true, если первый аргумент меньше второго или равен ему. Обратите внимание, значения могут быть только типа integer, float или string. Например, эта функция возвращает `true`: <p>`lessOrEquals(10,10)` <p> **Номер параметра:** 1. <p> **Имя:** объект 1. <p> **Описание:** обязательно. Объект для проверки, не меньше ли он **объекта 2** или равен ему. <p> **Номер параметра:** 2. <p> **Имя:** объект 2. <p> **Описание:** обязательно. Объект для проверки, не больше ли он **объекта 1** или равен ему.|  
|greater|Возвращает значение true, если первый аргумент больше второго. Обратите внимание, значения могут быть только типа integer, float или string. Например, эта функция возвращает `false`:  <p>`greater(10,10)` <p> **Номер параметра:** 1. <p> **Имя:** объект 1. <p> **Описание:** обязательно. Объект для проверки, не больше ли он **объекта 2**. <p> **Номер параметра:** 2. <p> **Имя:** объект 2. <p> **Описание:** обязательно. Объект для проверки, не меньше ли он **объекта 1**.|  
|greaterOrEquals|Возвращает значение true, если первый аргумент больше второго или равен ему. Обратите внимание, значения могут быть только типа integer, float или string. Например, эта функция возвращает `false`: <p>`greaterOrEquals(10,100)` <p> **Номер параметра:** 1. <p> **Имя:** объект 1. <p> **Описание:** обязательно. Объект для проверки, не больше ли он **объекта 2** или равен ему. <p> **Номер параметра:** 2. <p> **Имя:** объект 2. <p> **Описание:** обязательно. Объект для проверки, не меньше ли он **объекта 1** или равен ему.|  
|и|Возвращает значение true, если оба параметра получают значение true. Оба аргумента должны быть логическими значениями. Например, эта функция возвращает `false`: <p>`and(greater(1,10),equals(0,0))` <p> **Номер параметра:** 1. <p> **Имя:** логическое значение 1. <p> **Описание:** обязательно. Первый аргумент, который должен получить значение `true`. <p> **Номер параметра:** 2. <p> **Имя:** логическое значение 2. <p> **Описание:** обязательно. Второй аргумент, который должен получить значение `true`.|  
|или|Возвращает значение true, если какой-либо из параметров получает значение true. Оба аргумента должны быть логическими значениями. Например, эта функция возвращает `true`: <p>`or(greater(1,10),equals(0,0))` <p> **Номер параметра:** 1. <p> **Имя:** логическое значение 1. <p> **Описание:** обязательно. Первый аргумент, который может получить значение `true`. <p> **Номер параметра:** 2. <p> **Имя:** логическое значение 2. <p> **Описание:** обязательно. Второй аргумент, который может получить значение `true`.|  
|not|Возвращает значение true, если оба параметра получают значение `false`. Оба аргумента должны быть логическими значениями. Например, эта функция возвращает `true`: <p>`not(contains('200 Success','Fail'))` <p> **Номер параметра:** 1. <p> **Имя:** логическое значение. <p> **Описание:** возвращает значение true, если оба параметра получают значение `false`. Оба аргумента должны быть логическими значениями. Эта функция возвращает `true`: `not(contains('200 Success','Fail'))`|  
|if|Возвращает определенное значение, в зависимости от того, выполняется ли условие в выражении (`true` или `false`).  Например, эта функция возвращает `"yes"`: <p>`if(equals(1, 1), 'yes', 'no')` <p> **Номер параметра:** 1. <p> **Имя:** выражение. <p> **Описание:** обязательно. Логическое значение, определяющее, какое значение должно вернуться в результате обработки выражения. <p> **Номер параметра:** 2. <p> **Имя**: true. <p> **Описание:** обязательно. Значение, которое возвращается, если результатом обработки выражения является значение `true`. <p> **Номер параметра:** 3. <p> **Имя:** false. <p> **Описание:** обязательно. Значение, которое возвращается, если результатом обработки выражения является значение `false`.|  
  
### <a name="conversion-functions"></a>Функции преобразования  

Эти функции используются для преобразования между собственными типами языка:  
  
- строка  
  
- целое число  
  
- float;  
  
- Логическое  
  
- arrays;  
  
- dictionaries;  

-   forms.  
  
|Имя функции|ОПИСАНИЕ|  
|-------------------|-----------------|  
|int|Преобразование параметра в целое число. Например, эта функция возвращает 100 в виде числа, а не строки: <p>`int('100')` <p> **Номер параметра:** 1. <p> **Имя:** значение. <p> **Описание:** обязательно. Значение, которое преобразуется в целое число.|  
|строка|Преобразование параметра в строку. Например, эта функция возвращает `'10'`: <p>`string(10)` <p>Можно также преобразовать объект в строку. Например, если параметр `myPar` является объектом с одним свойством `abc : xyz`, то эта функция возвращает `{"abc" : "xyz"}`: <p>`string(parameters('myPar'))` <p> **Номер параметра:** 1. <p> **Имя:** значение. <p> **Описание:** обязательно. Значение, которое преобразуется в строку.|  
|json|Преобразование параметра в значение типа JSON. Эта функция является противоположностью `string()`. Например, эта функция возвращает `[1,2,3]` как массив, а не строку: <p>`json('[1,2,3]')` <p>Аналогичным образом можно преобразовать строку в объект. Например, эта функция возвращает `{ "abc" : "xyz" }`: <p>`json('{"abc" : "xyz"}')` <p> **Номер параметра:** 1. <p> **Имя:** строка. <p> **Описание:** обязательно. Строка, которая преобразуется в значение собственного типа. <p>Функция `json()` также поддерживает входные XML-данные. Например, значение параметра <p>`<?xml version="1.0"?> <root>   <person id='1'>     <name>Alan</name>     <occupation>Engineer</occupation>   </person> </root>` <p>преобразуется в такое значение JSON: <p>`{ "?xml": { "@version": "1.0" },   "root": {     "person": [     {       "@id": "1",       "name": "Alan",       "occupation": "Engineer"     }   ]   } }`|  
|float;|Преобразование аргумента параметра в число с плавающей запятой. Например, эта функция возвращает `10.333`: <p>`float('10.333')` <p> **Номер параметра:** 1. <p> **Имя:** значение. <p> **Описание:** обязательно. Значение, которое преобразуется в число с плавающей запятой.|  
|bool|Преобразование параметра в логическое значение. Например, эта функция возвращает `false`: <p>`bool(0)` <p> **Номер параметра:** 1. <p> **Имя:** значение. <p> **Описание:** обязательно. Значение, которое преобразуется в логическое значение.|  
|base64|Возвращает входную строку в кодировке Base64. Например, эта функция возвращает `c29tZSBzdHJpbmc=`: <p>`base64('some string')` <p> **Номер параметра:** 1. <p> **Имя:** строка 1. <p> **Описание:** обязательно. Строка, которую нужно кодировать в формате Base64.|  
|base64ToBinary|Возвращает двоичное представление строки в кодировке Base64. Например, эта функция возвращает двоичное представление значения `some string`: <p>`base64ToBinary('c29tZSBzdHJpbmc=')` <p> **Номер параметра:** 1. <p> **Имя:** строка. <p> **Описание:** обязательно. Строка в кодировке Base64.|  
|base64ToString|Возвращает строковое представление строки в кодировке Base64. Например, эта функция возвращает `some string`: <p>`base64ToString('c29tZSBzdHJpbmc=')` <p> **Номер параметра:** 1. <p> **Имя:** строка. <p> **Описание:** обязательно. Строка в кодировке Base64.|  
|Binary|Возвращает двоичное представление значения.  Например, эта функция возвращает двоичное представление значения `some string`: <p>`binary('some string')` <p> **Номер параметра:** 1. <p> **Имя:** значение. <p> **Описание:** обязательно. Значение, которое преобразуется в двоичный формат.|  
|dataUriToBinary|Возвращает двоичное представление URI данных. Например, эта функция возвращает двоичное представление значения `some string`: <p>`dataUriToBinary('data:;base64,c29tZSBzdHJpbmc=')` <p> **Номер параметра:** 1. <p> **Имя:** строка. <p> **Описание:** обязательно. URI данных для преобразования в двоичный формат.|  
|dataUriToString|Возвращает строковое представление URI данных. Например, эта функция возвращает `some string`: <p>`dataUriToString('data:;base64,c29tZSBzdHJpbmc=')` <p> **Номер параметра:** 1. <p> **Имя:** строка.<p> **Описание:** обязательно. URI данных для преобразования в строку.|  
|dataUri|Возвращает URI данных значения. Например, эта функция возвращает такой URI данных `text/plain;charset=utf8;base64,c29tZSBzdHJpbmc=`: <p>`dataUri('some string')` <p> **Номер параметра:** 1.<p> **Имя:** значение.<p> **Описание:** обязательно. Значение, которое необходимо преобразовать в URI данных.|  
|decodeBase64|Возвращает строковое представление входной строки в кодировке Base64. Например, эта функция возвращает `some string`: <p>`decodeBase64('c29tZSBzdHJpbmc=')` <p> **Номер параметра:** 1. <p> **Имя:** строка. <p> **Описание:** возвращает строковое представление входной строки в кодировке Base64.|  
|encodeUriComponent|Преобразование переданной строки в представление без опасных для URL-адреса символов. Например, эта функция возвращает `You+Are%3ACool%2FAwesome`: <p>`encodeUriComponent('You Are:Cool/Awesome')` <p> **Номер параметра:** 1. <p> **Имя:** строка. <p> **Описание:** обязательно. Строка, из которой нужно удалить символы, опасные для URL-адреса.|  
|decodeUriComponent|Преобразование переданной строки в представление с опасными для URL-адреса символами. Например, эта функция возвращает `You Are:Cool/Awesome`: <p>`decodeUriComponent('You+Are%3ACool%2FAwesome')` <p> **Номер параметра:** 1. <p> **Имя:** строка. <p> **Описание:** обязательно. Строка, из которой нужно декодировать опасные для URL-символы.|  
|decodeDataUri|Возвращает двоичное представление входной строки URI данных. Например, эта функция возвращает двоичное представление значения `some string`: <p>`decodeDataUri('data:;base64,c29tZSBzdHJpbmc=')` <p> **Номер параметра:** 1. <p> **Имя:** строка. <p> **Описание:** обязательно. URI данных, который нужно декодировать в двоичное представление.|  
|uriComponent|Возвращает закодированный URI. Например, эта функция возвращает `You+Are%3ACool%2FAwesome`: <p>`uriComponent('You Are:Cool/Awesome')` <p> **Номер параметра:** 1.<p> **Имя:** строка. <p> **Описание:** обязательно. Строка, в которой нужно закодировать URI.|  
|uriComponentToBinary|Возвращает двоичное представление строки с закодированным URI. Например, эта функция возвращает двоичное представление значения `You Are:Cool/Awesome`: <p>`uriComponentToBinary('You+Are%3ACool%2FAwesome')` <p> **Номер параметра:** 1. <p> **Имя:** строка.<p> **Описание:** обязательно. Строка с закодированным URI.|  
|uriComponentToString|Возвращает строковое представление строки с закодированным URI. Например, эта функция возвращает `You Are:Cool/Awesome`: <p>`uriComponentToString('You+Are%3ACool%2FAwesome')` <p> **Номер параметра:** 1.<p> **Имя:** строка.<p> **Описание:** обязательно. Строка с закодированным URI.|  
|xml|Возвращает XML-представление значения. Например, эта функция возвращает XML-содержимое, представленное `'\<name>Alan\</name>'`: <p>`xml('\<name>Alan\</name>')` <p>Функция `xml()` также поддерживает входные данные в виде объекта JSON. Например, параметр `{ "abc": "xyz" }` преобразуется в XML-содержимое: `\<abc>xyz\</abc>` <p> **Номер параметра:** 1.<p> **Имя:** значение.<p> **Описание:** обязательно. Значение, которое необходимо преобразовать в XML-формат.|  
|array|Преобразование параметра в массив. Например, эта функция возвращает `["abc"]`: <p>`array('abc')` <p> **Номер параметра:** 1. <p> **Имя:** значение. <p> **Описание:** обязательно. Значение, которое преобразуется в массив.|
|createArray|Создает массив из параметров. Например, эта функция возвращает `["a", "c"]`: <p>`createArray('a', 'c')` <p> **Номер параметра:** 1 ... *n*. <p> **Имя:** параметр с любым *номером*. <p> **Описание:** обязательно. Значения для объединения в массив.|
|triggerFormDataValue|Возвращает одно значение, соответствующее имени ключа, из выходных данных триггера формирования данных или кодировки данных в форме.  При наличии нескольких соответствий возникает ошибка.  Например, будет возвращено `bar`: `triggerFormDataValue('foo')`<br /><br />**Номер параметра:** 1.<br /><br />**Имя:** имя ключа.<br /><br />**Описание:** обязательно. Имя ключа возвращаемого значения данных формы.|
|triggerFormDataMultiValues|Возвращает массив значений, соответствующих имени ключа, из выходных данных триггера формирования данных или кодировки данных в форме.  Например, будет возвращено `["bar"]`: `triggerFormDataValue('foo')`<br /><br />**Номер параметра:** 1.<br /><br />**Имя:** имя ключа.<br /><br />**Описание:** обязательно. Имя ключа для возвращаемых значений данных формы.|
|triggerMultipartBody|Возвращает текст части в составных выходных данных триггера.<br /><br />**Номер параметра:** 1.<br /><br />**Имя:** индекс.<br /><br />**Описание:** обязательно. Индекс части для извлечения.|
|formDataValue|Возвращает одно значение, соответствующее имени ключа, из выходных данных действия формирования данных или кодировки данных в форме.  При наличии нескольких соответствий возникает ошибка.  Например, будет возвращено `bar`: `formDataValue('someAction', 'foo')`<br /><br />**Номер параметра:** 1.<br /><br />**Имя:** имя действия.<br /><br />**Описание:** обязательно. Имя действия с ответом о формировании данных или кодировке данных в форме.<br /><br />**Номер параметра:** 2.<br /><br />**Имя:** имя ключа.<br /><br />**Описание:** обязательно. Имя ключа возвращаемого значения данных формы.|
|formDataMultiValues|Возвращает массив значений, соответствующих имени ключа, из выходных данных действия формирования данных или кодировки данных в форме.  Например, будет возвращено `["bar"]`: `formDataMultiValues('someAction', 'foo')`<br /><br />**Номер параметра:** 1.<br /><br />**Имя:** имя действия.<br /><br />**Описание:** обязательно. Имя действия с ответом о формировании данных или кодировке данных в форме.<br /><br />**Номер параметра:** 2.<br /><br />**Имя:** имя ключа.<br /><br />**Описание:** обязательно. Имя ключа для возвращаемых значений данных формы.|
|multipartBody|Возвращает текст части в составных выходных данных действия.<br /><br />**Номер параметра:** 1.<br /><br />**Имя:** имя действия.<br /><br />**Описание:** обязательно. Имя действия с составным ответом.<br /><br />**Номер параметра:** 2.<br /><br />**Имя:** индекс.<br /><br />**Описание:** обязательно. Индекс части для извлечения.|

### <a name="manipulation-functions"></a>Функции обработки
 
Эти функции применяются к XML-файлам и объектам.
 
|Имя функции|ОПИСАНИЕ|  
|-------------------|-----------------| 
|coalesce|Возвращает первый объект, который не имеет значение null, в переданных аргументах. **Примечание.** Пустая строка не имеет значение null. Например, если параметры 1 и 2 не определены, эта функция возвращает `fallback`:  <p>`coalesce(parameters('parameter1'), parameters('parameter2') ,'fallback')` <p> **Номер параметра:** 1 ... *n*. <p> **Имя:** объект *n*. <p> **Описание:** обязательно. Объекты, значения которых проверяются на соответствие значению null.|
|addProperty|Возвращает объект с дополнительными свойствами. Если свойство уже существует во время выполнения, возникает ошибка. Например, эта функция возвращает объект `{ "abc" : "xyz", "def": "uvw" }`: <p>`addProperty(json('{"abc" : "xyz"}'), 'def', 'uvw')` <p> **Номер параметра:** 1. <p> **Имя:** объект. <p> **Описание:** обязательно. Объект, для которого нужно добавить новое свойство. <p> **Номер параметра:** 2. <p> **Имя:** имя свойства. <p> **Описание:** обязательно. Имя нового свойства. <p> **Номер параметра:** 3. <p> **Имя:** значение. <p> **Описание:** обязательно. Значение, присваиваемое новому свойству.|
|setProperty|Возвращает объект с дополнительным свойством или имеющийся набор свойств для данного значения. Например, эта функция возвращает объект `{ "abc" : "uvw" }`: <p>`setProperty(json('{"abc" : "xyz"}'), 'abc', 'uvw')` <p> **Номер параметра:** 1. <p> **Имя:** объект. <p> **Описание:** обязательно. Объект, в котором необходимо задать свойство.<p> **Номер параметра:** 2. <p> **Имя:** имя свойства.<p> **Описание:** обязательно. Имя нового или имеющегося свойства. <p> **Номер параметра:** 3. <p> **Имя:** значение. <p> **Описание:** обязательно. Значение, присваиваемое свойству.|
|removeProperty|Возвращает объект с удаленным свойством. Если удаляемого свойства нет, возвращается исходный объект. Например, эта функция возвращает объект `{ "abc" : "xyz" }`: <p>`removeProperty(json('{"abc" : "xyz", "def": "uvw"}'), 'def')` <p> **Номер параметра:** 1. <p> **Имя:** объект. <p> **Описание:** обязательно. Объект, из которого необходимо удалить свойство.<p> **Номер параметра:** 2. <p> **Имя:** имя свойства. <p> **Описание:** обязательно. Имя удаляемого свойства. <p>|
|xpath|Возвращает массив XML-узлов, соответствующих значению, которое возвращает выражение XPath. <p> **Пример 1** <p>Предположим, значение параметра `p1` — строковое представление этого XML-кода: <p>`<?xml version="1.0"?> <lab>   <robot>     <parts>5</parts>     <name>R1</name>   </robot>   <robot>     <parts>8</parts>     <name>R2</name>   </robot> </lab>` <p>Код `xpath(xml(parameters('p1')), '/lab/robot/name')` <p>возвращает <p>`[ <name>R1</name>, <name>R2</name> ]` <p>А код <p>`xpath(xml(parameters('p1')), ' sum(/lab/robot/parts)')` <p>возвращает <p>`13` <p> <p> **Пример 2** <p>У нас есть следующее XML-содержимое: <p>`<?xml version="1.0"?> <File xmlns="http://foo.com">   <Location>bar</Location> </File>` <p>Код `@xpath(xml(body('Http')), '/*[name()=\"File\"]/*[name()=\"Location\"]')` <p>или код <p>`@xpath(xml(body('Http')), '/*[local-name()=\"File\" and namespace-uri()=\"http://foo.com\"]/*[local-name()=\"Location\" and namespace-uri()=\"\"]')` <p>возвращает <p>`<Location xmlns="http://abc.com">xyz</Location>` <p>А код `@xpath(xml(body('Http')), 'string(/*[name()=\"File\"]/*[name()=\"Location\"])')` <p>возвращает <p>``xyz`` <p> **Номер параметра:** 1. <p> **Имя:** XML. <p> **Описание:** обязательно. XML-код, который будет использоваться для вычисления выражения XPath. <p> **Номер параметра:** 2. <p> **Имя:** XPath. <p> **Описание:** обязательно. Выражение XPath для вычисления.|

### <a name="math-functions"></a>Математические функции  

Эти функции могут использоваться для любого типа чисел: **целых чисел** и **чисел с плавающей запятой**.  
  
|Имя функции|ОПИСАНИЕ|  
|-------------------|-----------------|  
|добавление|Возвращает результат сложения двух чисел. Например, эта функция возвращает `20.333`: <p>`add(10,10.333)` <p> **Номер параметра:** 1. <p> **Имя:** слагаемое 1. <p> **Описание:** обязательно. Число для добавления к **слагаемому 2**. <p> **Номер параметра:** 2. <p> **Имя:** слагаемое 2. <p> **Описание:** обязательно. Число для добавления к **слагаемому 1**.|  
|sub|Возвращает результат вычитания двух чисел. Например, эта функция возвращает `-0.333`: <p>`sub(10,10.333)` <p> **Номер параметра:** 1. <p> **Имя:** уменьшаемое. <p> **Описание:** обязательно. Число, из которого вычитается **вычитаемое**. <p> **Номер параметра:** 2. <p> **Имя:** вычитаемое. <p> **Описание:** обязательно. Число, которое вычитается из **уменьшаемого**.|  
|mul|Возвращает результат умножения двух чисел. Например, эта функция возвращает `103.33`: <p>`mul(10,10.333)` <p> **Номер параметра:** 1. <p> **Имя:** множитель 1. <p> **Описание:** обязательно. Число, на которое умножается **множитель 2**. <p> **Номер параметра:** 2. <p> **Имя:** множитель 2. <p> **Описание:** обязательно. Число, на которое умножается **множитель 1**.|  
|div|Возвращает результат деления двух чисел. Например, эта функция возвращает `1.0333`: <p>`div(10.333,10)` <p> **Номер параметра:** 1. <p> **Имя:** делимое. <p> **Описание:** обязательно. Число, которое нужно поделить на **делитель**. <p> **Номер параметра:** 2. <p> **Имя:** делитель. <p> **Описание:** обязательно. Число, на которое нужно поделить **делимое**.|  
|mod (модуль)|Возвращает остаток после деления двух чисел (остаток от деления). Например, эта функция возвращает `2`: <p>`mod(10,4)` <p> **Номер параметра:** 1. <p> **Имя:** делимое. <p> **Описание:** обязательно. Число, которое нужно поделить на **делитель**. <p> **Номер параметра:** 2. <p> **Имя:** делитель. <p> **Описание:** обязательно. Число, на которое нужно поделить **делимое**. После деления берется остаток.|  
|Min|Существует два разных метода вызова этой функции. <p>`min` принимает массив и возвращает значение `0`: <p>`min([0,1,2])` <p>Кроме того, эта функция может принять разделенный запятыми список значений и также вернуть `0`: <p>`min(0,1,2)` <p> **Примечание.** Все значения должны быть числами, поэтому если параметр представляет собой массив, этот массив должен содержать только числа. <p> **Номер параметра:** 1. <p> **Имя:** коллекция или значение. <p> **Описание:** обязательно. Массив значений для поиска минимального значения или первого значения набора. <p> **Номер параметра:** 2 ... *n*. <p> **Имя:** значение *n*. <p> **Описание:** необязательно. Если первый параметр является значением, можно передать дополнительные значения. В таком случае будет возвращено минимальное из всех переданных значений.|  
|max|Существует два разных метода вызова этой функции. <p>`max` принимает массив и возвращает значение `2`: <p>`max([0,1,2])` <p>Кроме того, эта функция может принять разделенный запятыми список значений и также вернуть `2`: <p>`max(0,1,2)` <p> **Примечание.** Все значения должны быть числами, поэтому если параметр представляет собой массив, этот массив должен содержать только числа. <p> **Номер параметра:** 1. <p> **Имя:** коллекция или значение. <p> **Описание:** обязательно. Массив значений для поиска максимального значения или первого значения набора. <p> **Номер параметра:** 2 ... *n*. <p> **Имя:** значение *n*. <p> **Описание:** необязательно. Если первый параметр является значением, можно передать дополнительные значения. В таком случае будет возвращено максимальное из всех переданных значений.|  
|range|Создает массив целых чисел, начиная с определенного числа. При этом нужно определить длину возвращаемого массива. <p>Например, эта функция возвращает `[3,4,5,6]`: <p>`range(3,4)` <p> **Номер параметра:** 1. <p> **Имя:** индекс начала. <p> **Описание:** обязательно. Первое целое число в массиве. <p> **Номер параметра:** 2. <p> **Имя:** число. <p> **Описание:** обязательно. Это значение представляет собой набор целых чисел, содержащихся в массиве.|  
|rand|Создает случайное целое число в указанном диапазоне (включая только начальное значение диапазона). Например, эта функция может вернуть `0` или 1. <p>`rand(0,2)` <p> **Номер параметра:** 1. <p> **Имя:** минимальное значение. <p> **Описание:** обязательно. Наименьшее целое число, которое может быть возвращено. <p> **Номер параметра:** 2. <p> **Имя:** максимальное значение. <p> **Описание:** обязательно. Это целое число, которое следует после наибольшего целого числа, которое может быть возвращено.|  
 
### <a name="date-functions"></a>Функции данных  

|Имя функции|ОПИСАНИЕ|  
|-------------------|-----------------|  
|utcnow|Возвращает текущую метку времени в виде строки, например `2017-03-15T13:27:36Z`: <p>`utcnow()` <p> **Номер параметра:** 1. <p> **Имя:** формат. <p> **Описание:** необязательно. [Один допустимый символ описателя формата](https://msdn.microsoft.com/library/az4se3k1%28v=vs.110%29.aspx) или [пользовательский шаблон формата](https://msdn.microsoft.com/library/8kb3ddd4%28v=vs.110%29.aspx), который определяет способ форматирования значения этой метки времени. Если формат не указан, используется формат ISO 8601 ("o").|  
|addseconds|Добавляет число секунд (целое) к переданной метке времени (строка). Это число может быть положительным или отрицательным. По умолчанию результатом является строка в формате ISO 8601 ("o"), если описатель формата не указан. Например, `2015-03-15T13:27:00Z`: <p>`addseconds('2015-03-15T13:27:36Z', -36)` <p> **Номер параметра:** 1. <p> **Имя:** метка времени. <p> **Описание:** обязательно. Строка, содержащая время. <p> **Номер параметра:** 2. <p> **Имя:** секунды. <p> **Описание:** обязательно. Количество секунд, которое нужно добавить. Это значение может быть отрицательным для вычитания секунд. <p> **Номер параметра:** 3. <p> **Имя:** формат. <p> **Описание:** необязательно. [Один допустимый символ описателя формата](https://msdn.microsoft.com/library/az4se3k1%28v=vs.110%29.aspx) или [пользовательский шаблон формата](https://msdn.microsoft.com/library/8kb3ddd4%28v=vs.110%29.aspx), который определяет способ форматирования значения этой метки времени. Если формат не указан, используется формат ISO 8601 ("o").|  
|addminutes|Добавляет число минут (целое) к переданной метке времени (строка). Это число может быть положительным или отрицательным. По умолчанию результатом является строка в формате ISO 8601 ("o"), если описатель формата не указан. Например, `2015-03-15T14:00:36Z`: <p>`addminutes('2015-03-15T13:27:36Z', 33)` <p> **Номер параметра:** 1. <p> **Имя:** метка времени. <p> **Описание:** обязательно. Строка, содержащая время. <p> **Номер параметра:** 2. <p> **Имя:** минуты. <p> **Описание:** обязательно. Количество минут, которое нужно добавить. Это значение может быть отрицательным для вычитания минут. <p> **Номер параметра:** 3. <p> **Имя:** формат. <p> **Описание:** необязательно. [Один допустимый символ описателя формата](https://msdn.microsoft.com/library/az4se3k1%28v=vs.110%29.aspx) или [пользовательский шаблон формата](https://msdn.microsoft.com/library/8kb3ddd4%28v=vs.110%29.aspx), который определяет способ форматирования значения этой метки времени. Если формат не указан, используется формат ISO 8601 ("o").|  
|addhours|Добавляет число часов (целое) к переданной метке времени (строка). Это число может быть положительным или отрицательным. По умолчанию результатом является строка в формате ISO 8601 ("o"), если описатель формата не указан. Например, `2015-03-16T01:27:36Z`: <p>`addhours('2015-03-15T13:27:36Z', 12)` <p> **Номер параметра:** 1. <p> **Имя:** метка времени. <p> **Описание:** обязательно. Строка, содержащая время. <p> **Номер параметра:** 2. <p> **Имя:** часы. <p> **Описание:** обязательно. Количество часов, которое нужно добавить. Это значение может быть отрицательным для вычитания часов. <p> **Номер параметра:** 3. <p> **Имя:** формат. <p> **Описание:** необязательно. [Один допустимый символ описателя формата](https://msdn.microsoft.com/library/az4se3k1%28v=vs.110%29.aspx) или [пользовательский шаблон формата](https://msdn.microsoft.com/library/8kb3ddd4%28v=vs.110%29.aspx), который определяет способ форматирования значения этой метки времени. Если формат не указан, используется формат ISO 8601 ("o").|  
|adddays|Добавляет число дней (целое) к переданной метке времени (строка). Это число может быть положительным или отрицательным. По умолчанию результатом является строка в формате ISO 8601 ("o"), если описатель формата не указан. Например, `2015-03-13T13:27:36Z`: <p>`adddays('2015-03-15T13:27:36Z', -2)` <p> **Номер параметра:** 1. <p> **Имя:** метка времени. <p> **Описание:** обязательно. Строка, содержащая время. <p> **Номер параметра:** 2. <p> **Имя:** дни. <p> **Описание:** обязательно. Количество дней, которое нужно добавить. Это значение может быть отрицательным для вычитания дней. <p> **Номер параметра:** 3. <p> **Имя:** формат. <p> **Описание:** необязательно. [Один допустимый символ описателя формата](https://msdn.microsoft.com/library/az4se3k1%28v=vs.110%29.aspx) или [пользовательский шаблон формата](https://msdn.microsoft.com/library/8kb3ddd4%28v=vs.110%29.aspx), который определяет способ форматирования значения этой метки времени. Если формат не указан, используется формат ISO 8601 ("o").|  
|formatDateTime|Возвращает строку в формате даты. По умолчанию результатом является строка в формате ISO 8601 ("o"), если описатель формата не указан. Например, `2015-03-15T13:27:36Z`: <p>`formatDateTime('2015-03-15T13:27:36Z', 'o')` <p> **Номер параметра:** 1. <p> **Имя:** дата. <p> **Описание:** обязательно. Строка, содержащая дату. <p> **Номер параметра:** 2. <p> **Имя:** формат. <p> **Описание:** необязательно. [Один допустимый символ описателя формата](https://msdn.microsoft.com/library/az4se3k1%28v=vs.110%29.aspx) или [пользовательский шаблон формата](https://msdn.microsoft.com/library/8kb3ddd4%28v=vs.110%29.aspx), который определяет способ форматирования значения этой метки времени. Если формат не указан, используется формат ISO 8601 ("o").|  
|startOfHour|Возвращает начало часа к переданной метке времени (строка). Например, `2017-03-15T13:00:00Z`:<br /><br /> `startOfHour('2017-03-15T13:27:36Z')`<br /><br /> **Номер параметра:** 1.<br /><br /> **Имя:** метка времени.<br /><br /> **Описание:** обязательно. Это строка, содержащая время.<br /><br />**Номер параметра:** 2.<br /><br /> **Имя:** формат.<br /><br /> **Описание:** необязательно. [Один допустимый символ описателя формата](https://msdn.microsoft.com/library/az4se3k1%28v=vs.110%29.aspx) или [пользовательский шаблон формата](https://msdn.microsoft.com/library/8kb3ddd4%28v=vs.110%29.aspx), который определяет способ форматирования значения этой метки времени. Если формат не указан, используется формат ISO 8601 ("o").|  
|startOfDay|Возвращает начало дня к переданной метке времени (строка). Например, `2017-03-15T00:00:00Z`:<br /><br /> `startOfDay('2017-03-15T13:27:36Z')`<br /><br /> **Номер параметра:** 1.<br /><br /> **Имя:** метка времени.<br /><br /> **Описание:** обязательно. Это строка, содержащая время.<br /><br />**Номер параметра:** 2.<br /><br /> **Имя:** формат.<br /><br /> **Описание:** необязательно. [Один допустимый символ описателя формата](https://msdn.microsoft.com/library/az4se3k1%28v=vs.110%29.aspx) или [пользовательский шаблон формата](https://msdn.microsoft.com/library/8kb3ddd4%28v=vs.110%29.aspx), который определяет способ форматирования значения этой метки времени. Если формат не указан, используется формат ISO 8601 ("o").| 
|startOfMonth|Возвращает начало месяца для переданной метки времени (строка). Например, `2017-03-01T00:00:00Z`:<br /><br /> `startOfMonth('2017-03-15T13:27:36Z')`<br /><br /> **Номер параметра:** 1.<br /><br /> **Имя:** метка времени.<br /><br /> **Описание:** обязательно. Это строка, содержащая время.<br /><br />**Номер параметра:** 2.<br /><br /> **Имя:** формат.<br /><br /> **Описание:** необязательно. [Один допустимый символ описателя формата](https://msdn.microsoft.com/library/az4se3k1%28v=vs.110%29.aspx) или [пользовательский шаблон формата](https://msdn.microsoft.com/library/8kb3ddd4%28v=vs.110%29.aspx), который определяет способ форматирования значения этой метки времени. Если формат не указан, используется формат ISO 8601 ("o").| 
|dayOfWeek|Возвращает компонент дня недели из метки времени (строка).  0 — воскресенье, понедельник — 1 и т. д. Например, `3`:<br /><br /> `dayOfWeek('2017-03-15T13:27:36Z')`<br /><br /> **Номер параметра:** 1.<br /><br /> **Имя:** метка времени.<br /><br /> **Описание:** обязательно. Это строка, содержащая время.| 
|dayOfMonth|Возвращает компонент дня месяца из строки метки времени. Например, `15`:<br /><br /> `dayOfMonth('2017-03-15T13:27:36Z')`<br /><br /> **Номер параметра:** 1.<br /><br /> **Имя:** метка времени.<br /><br /> **Описание:** обязательно. Это строка, содержащая время.| 
|dayOfYear|Возвращает компонент дня года из строки метки времени. Например, `74`:<br /><br /> `dayOfYear('2017-03-15T13:27:36Z')`<br /><br /> **Номер параметра:** 1.<br /><br /> **Имя:** метка времени.<br /><br /> **Описание:** обязательно. Это строка, содержащая время.| 
|ticks|Возвращает свойство тактов из строки метки времени. Например, `1489603019`:<br /><br /> `ticks('2017-03-15T18:36:59Z')`<br /><br /> **Номер параметра:** 1.<br /><br /> **Имя:** метка времени.<br /><br /> **Описание:** обязательно. Это строка, содержащая время.| 
  
### <a name="workflow-functions"></a>Функции рабочего процесса  

Эти функции позволяют получить сведения о самом рабочем процессе во время выполнения.  
  
|Имя функции|ОПИСАНИЕ|  
|-------------------|-----------------|  
|listCallbackUrl|Возвращает строку для вызова триггера или действия. <p> **Примечание.** Эта функция может использоваться только в **httpWebhook** и **apiConnectionWebhook**, но не в **manual**, **recurrence**, **http** или **apiConnection**. <p>Например, функция `listCallbackUrl()` возвращает: <p>`https://prod-01.westus.logic.azure.com:443/workflows/1235...ABCD/triggers/manual/run?api-version=2015-08-01-preview&sp=%2Ftriggers%2Fmanual%2Frun&sv=1.0&sig=xxx...xxx` |  
|рабочий процесс|Эта функция предоставляет подробные сведения о самом рабочем процессе во время выполнения. <p> К доступным свойствам объекта рабочего процесса относятся: <ul><li>`name`</li><li>`type`</li><li>`id`</li><li>`location`</li><li>`run`</li></ul> <p> Значение свойства `run` представляет собой объект со следующими свойствами: <ul><li>`name`</li><li>`type`</li><li>`id`</li></ul> <p>Сведения об этих свойствах см. в статье для [Rest API](http://go.microsoft.com/fwlink/p/?LinkID=525617).<p> Например, чтобы получить имя текущего выполнения, используйте выражение `"@workflow().run.name"`. |

## <a name="next-steps"></a>Дополнительная информация

[Действия и триггеры рабочего процесса](logic-apps-workflow-actions-triggers.md)
